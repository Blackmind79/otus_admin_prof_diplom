# code: language=yaml
# playbooks/playbook_nexus.yaml
---
- name: "Configuration [nexus]"
  vars_files:
    - ../.vault.enc
  hosts:
    - nexus
  become: true
  gather_facts: true
  roles:
    - role: role_nexus
      when: ansible_facts["system"] | lower == "linux"